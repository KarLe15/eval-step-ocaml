# Interface pour l'evaluateur OCaml

## Build 
- Installer `node` https://nodejs.org/en/ 
- Installer les dépendances : npm install
- Taper `npm start`

ouvrir un navigateur : `http://localhost:4200/`
